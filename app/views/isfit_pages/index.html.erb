<h1>Pages on isfit.org</h1>
<p>Below is a list of articles currently stored for isfit.org.</p>

<p><%= link_to 'New Isfit page', new_isfit_page_path, class: "big primary btn" %></p>
<table class="zebra-striped sortable">
  <tr>
    <th>English title</th>
    <th>Norwegian title</th>
    <th>Action</th>
  </tr>

<% @isfit_pages.each do |isfit_page| %>
  <tr>
    <td><%= isfit_page.title_en %></td>
    <td><%= isfit_page.title_no %></td>
    <td><%= link_to 'Show', isfit_page, class: "primary btn" %>
    <%= link_to 'Edit', edit_isfit_page_path(isfit_page), class: "btn" %>
    <%= link_to 'Destroy', isfit_page, confirm: 'Are you sure?', method: :delete , class: "danger btn"%></td>
  </tr>
<% end %>
</table>

<br />

