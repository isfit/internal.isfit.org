<h2>Festivalens beste</h2>

<h3>Hvem har flest riktige?</h3>
<ul style="list-style-type: decimal";>
  <% @highscorer.points_sorted.each do |user| %>
    <li><%= link_to user.full_name, user %> (<%= user.score %>)</li>
  <% end %>
</ul>

<h3>Hvilken bruker er best prosent-vis?</h3>
<p> (Du må ha 10 eller flere spill for å bli med i listen) </p>
<ul style="list-style-type: decimal";>
  <% @highscorer.best_ratio_sorted.each do |user| %>
    <li>
      <%= link_to user.full_name, user %> ->
      <%= number_to_percentage(user.ratio * 100, precision: 2) %>
      in <%= user.played_sum %> played
    </li>
  <% end %>
</ul>

<h3>Hvem husker ISFiT best?</h3>
<ul style="list-style-type: decimal";>
  <% @highscorer.best_known_users.each do |user| %>
    <li>
      <%= link_to user.full_name, user %> ->
      <%= number_to_percentage(user.ratio * 100, precision: 2) %>
    </li>
  <% end %>
</ul>
