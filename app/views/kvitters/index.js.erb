$('#kvitter-index').append('<%= j render(partial: "kvitter", collection: @kvitters) %>');
<% if @kvitters.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate(@kvitters) %>');
<% else %>
  $('.pagination').remove();
<% end %>
