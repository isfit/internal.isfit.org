<h1>Articles</h1>
<p>Below is a list of all articles available for studentpeaceprize.org. <br />
Article on top will be listed as main article on studentpeaceprize.org, and the 6 next will also be on the front page <br />
The rest of the articles will be listed under archive</p>
<div class="action-menu">
  <%= link_to "Create article", new_editorial_spp_article_path, class: "btn-primary btn" %>
</div>
<table class="table table-bordered table-striped">
  <thead>
    <tr>
      <th>English title</th>
      <th>Norwegian title</th>
      <th>Created</th>
      <th>Priority</th>
      <th></th>
    </tr>
  </thead>
  <% @spp_articles.each do |article| %>
    <tr>
      <td><%= article.title_en.blank? ? "<em>No English version</em>".html_safe : article.title_en %> </td>
      <td><%= article.title_no.blank? ? "<em>No Norwegian version</em>".html_safe : article.title_no  %></td>
      <td><%= article.created_at.strftime("%d %b %Y") %></td>
      <td><%= link_to("up", moveup_editorial_spp_article_path(article), class: "btn")%> <%=link_to("down", movedown_editorial_spp_article_path(article), class: "btn")%></td>
      <td width="100px">
        <div class="btn-group">
          <%= link_to("<i class='icon-pencil'></i> Edit".html_safe, edit_editorial_spp_article_path(article), class: "btn btn-info") %>
          <a href="#" class="btn btn-info dropdown-toggle" data-toggle="dropdown"><span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to "<i class='icon-file'></i> Show".html_safe, [:editorial,article] %></li>
            <li><%= link_to("<i class='icon-remove'></i> Delete".html_safe, [:editorial,article], method: :delete, confirm: 'Are you sure you want to delete this article?') %></li>
          </ul>
        </div>
      </td>
    </tr>
  <% end %>
</table>
