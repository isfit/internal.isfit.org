<h1>Articles</h1>
<p>Below is a list of all articles available for isfit.org. <br />
Article on top will be listed as main article on isfit.org, and the 6 next will also be on the front page <br />
The rest of the articles will be listed under archive</p>
<div class="pull-right">
  <%= link_to "Create article", new_editorial_frontend_article_path, class: "btn btn-primary" %>
</div>
<table class="table table-bordered table-striped">
  <thead>
  <tr>
    <th>English title</th>
    <th>Norwegian title</th>
    <th>Created</th>
    <th>Priority</th>
    <th>Published</th>
    <th>Type</th>
    <th></th>
  </tr>
</thead>
  <% @articles.each do |article| %>
    <tr>
      <td><%= article.title_en.blank? ? "<em>No English version</em>".html_safe : article.title_en %> </td>
      <td><%= article.title_no.blank? ? "<em>No Norwegian version</em>".html_safe : article.title_no  %></td>
		  <td><%= article.created_at.strftime("%d %b %Y") %></td>
		  <td><%= link_to("up", moveup_editorial_frontend_article_path(article), class: "btn")%> <%=link_to("down", movedown_editorial_frontend_article_path(article), class: "btn")%></td>
		  <td><%= article.list ? "Yes" : "No" %></td>
      <td><%= article.blog ? "Blog" : "Article" %></td>
      <td width="70px">
        <div class="btn-group">
          <%= link_to("Edit", edit_editorial_frontend_article_path(article), class: "btn btn-info") %>
          <a href="#" class="btn btn-info dropdown-toggle" data-toggle="dropdown"><span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to "Show", [:editorial,article] %></li>
            <li><%= link_to("Delete", [:editorial,article],  method: :delete, confirm: 'Are you sure you want to delete this article?') %></li>
          </ul>
        </div>
      </td>


    </tr>
  <% end %>
</table>

