<h1>Articles</h1>
<p>Below is a list of all articles available for isfit.org. <br />
Article on top will be listed as main article on isfit.org, and the 6 next will also be on the front page <br />
The rest of the articles will be listed under archive</p>
<div class="action-menu">
  <%= link_to "Create article", new_frontend_article_path, class: "primary btn" %>
</div>
<table class="bordered-table zebra-striped">
  <tr>
    <th>English title</th>
    <th>Norwegian title</th>
    <th>Created</th>
    <th>Priority</th>
    <th>Published</th>
    <th></th>
  </tr>
  <% @articles.each do |article| %>
    <tr>
      <td><%= article.title_en.blank? ? "<em>No English version</em>".html_safe : article.title_en %> </td>
      <td><%= article.title_no.blank? ? "<em>No Norwegian version</em>".html_safe : article.title_no  %></td>
		  <td><%= article.created_at.strftime("%d %b %Y") %></td>
		  <td><%= link_to("up", moveup_frontend_article_path(article), class: "btn")%> <%=link_to("down", movedown_frontend_article_path(article), class: "btn")%></td>
		  <td><%= article.list ? "Yes" : "No" %></td>
      <td><%= link_to "Show", article, class: "btn info" %>
          <%= link_to("Edit", edit_frontend_article_path(article), class: "btn") %>
          <%= link_to("Delete", article, class: "btn danger", method: :delete, confirm: 'Are you sure you want to delete this article?') %>
      </td>
    </tr>
  <% end %>
</table>

