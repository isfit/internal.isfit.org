<h1>Who am I?</h1>
<div class="pull-right">
	<a href="highscore" class="btn btn-large btn-primary">Gå til highscorelisten</button></a>
</div>

<p><b><b/></p>
<div class="picture">
  <%= image_tag @correct_user.profile_picture.url(:large) %>
</div>
<hr />
<br>
<div id="what_guesses" class="what_seperate">
	<% @players.each_with_index do |user, index| %>
		<% if index == 2 %>
    	<br>
    <% end %>
		<form method="POST" >
			<%= form_tag %>
			<input type="hidden" name="session" value="<%= @game.id%>">
			<input type="hidden" name="user_id" value="<%= user.id%>">
			<input class="span3 btn btn-large btn-info" type="submit" name="user_name" value="<%= user.full_name %>">
		</form>
	<% end %>
</div>
