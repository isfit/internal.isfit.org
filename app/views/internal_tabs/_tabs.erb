<ul>
  <% tabs.each do |tab, children| %>
    <% url = url_for_arranged_tab(children) %>
    <li><%= link_to tab.title.capitalize, url if url%></li>
  <% end %>
  <li><%= link_to('Log out', logout_path) %></li>
</ul>
