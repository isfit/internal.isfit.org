<% sublinks.each do |sublink| %>
  <%
    url = url_for_internal_tab(sublink)
    active =sublink == current_sublink
    %>
  
  <%= link_to sublink.title, url, :class => active ? "a_sublink" : "ina_sublink"  if can_access_link(sublink) %>
<% end %>
